import { Reference } from "tanstack_start_ts";

// Reference renders the full Ngeli (noun-class) reference table from the app's
// own static data — no props, no fetching.
export const NgeliTable = () => <Reference />;
