import { CardsTab } from "tanstack_start_ts";

// CardsTab loads the user's vocab from local storage and renders the lexicon
// card list. No props — it is a screen-level container.
export const Cards = () => <CardsTab />;
